node_modules
.next
out
next-env.d.ts
*.tsbuildinfo
.DS_Store

# Local env — real values live here; commit .env.example instead
.env
.env.local
.env*.local

# Production env IS committed: only NEXT_PUBLIC_* (browser-safe) values live here
!.env.production

# Claude Code local settings
.claude/settings.local.json
