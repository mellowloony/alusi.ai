# Supabase — both values are browser-safe (they ship in the client bundle).
# The publishable key uses the anon role; row-level security governs access.
NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxxxxxxxxxxxxxxxxxxxxxxx
